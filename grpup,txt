  const reports = await SalonReport.aggregate([
    { $match: { user: user._id } },
    {
      $group: {
        _id: {
          name: "$name",
          //category: "$category",
        },
        // count: {
        //   $sum: 1,
        // },
      },
    },
  ])
    .skip(0)
    .limit(5);